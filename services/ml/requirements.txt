# Pinned to the versions this layer was trained and tested against: a model's
# metrics are only reproducible against the framework that produced them, and the
# checkpoint records its torch version for the same reason.
torch==2.13.0
numpy==2.5.2
pyarrow==25.0.1
psycopg2-binary==2.9.12
boto3==1.34.0
